name     : Load Paired-End Reads From Web
subtitle : Upload a Paired End Library into your Narrative
tooltip  : Upload a Paired End Library into your Narrative

screenshots :
    []

suggestions :
    apps:
        related :
            []
        next :
            []
    methods:
        related :
            []
        next :
            []

parameters :

    download_type :
        ui-name : Download Type
        short-hint : Download type ('Direct Download Link', 'FTP Link', 'DropBox Public Shared Link',  'Google Drive Public Shared Link')
        long-hint  : Download type ('Direct Download Link', 'FTP Link', 'DropBox Public Shared Link',  'Google Drive Public Shared Link')

    fwd_file_url :
        ui-name : Forward/Left Fasta/Fastq URL
        short-hint : Download link containing a single end library in FASTA/FASTQ format
        long-hint  : |
            Valid file extensions for FASTA: .fasta, .fna, .fa   Valid file extensions for FASTQ: .fastq, .fnq, .fq; Compressed files (containing files with vaild extentions): .zip, .gz, .bz2, .tar.gz, .tar.bz2

    rev_file_url :
        ui-name : Reverse/Right Fasta/Fastq URL
        short-hint : Second download link containing a single end library in FASTA/FASTQ format
        long-hint  : |
            Valid file extensions for FASTA: .fasta, .fna, .fa   Valid file extensions for FASTQ: .fastq, .fnq, .fq; Compressed files (containing files with vaild extentions): .zip, .gz, .bz2, .tar.gz, .tar.bz2

    sequencing_tech :
        ui-name : Sequencing Technology
        short-hint : The name of the sequencing technology used to create the reads file
        long-hint  : The name of the sequencing technology used to create the reads file

    name :
        ui-name : Output Object File Name
        short-hint : Provide a name for the output object that will be created by this upload
        long-hint  : Provide a name for the output object that will be created by this upload

    single_genome :
        ui-name: Single Genome
        short-hint: Whether the reads are from a single genome or a metagenome.
        long-hint: Whether the reads are from a single genome or a metagenome.

    insert_size_mean :
        ui-name : Mean Insert Size
        short-hint : The mean (average) insert length
        long-hint  : The mean (average) insert length

    insert_size_std_dev :
        ui-name : Stdev Of Insert Size
        short-hint : The standard deviation of insert lengths
        long-hint  : The standard deviation of insert lengths

    read_orientation_outward :
        ui-name : Reads Orientation Outward
        short-hint : Whether reads in a pair point outward
        long-hint  : Whether reads in a pair point outward

parameter-groups :
    urls_to_add :
        ui-name : URL(s)
        short-hint : Fastq file URL and output Reads file name
        long-hint  : Fastq file URL and output Reads file name

description : |
    <p> Upload a Paired End Library into your Narrative </p>

technical-description : none